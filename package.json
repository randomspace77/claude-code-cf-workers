{
  "name": "claude-code-proxy-cf-workers",
  "version": "1.0.0",
  "type": "module",
  "description": "A Cloudflare Workers proxy that enables Claude Code to work with OpenAI-compatible API providers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "build:node": "esbuild src/node-server.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist-node/server.mjs",
    "start:node": "node dist-node/server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json",
    "cf-typegen": "wrangler types"
  },
  "keywords": [
    "claude",
    "openai",
    "proxy",
    "cloudflare-workers",
    "api"
  ],
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.3",
    "@cloudflare/workers-types": "^4.20260410.1",
    "@types/node": "^25.6.0",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4",
    "wrangler": "^4.81.1"
  }
}
